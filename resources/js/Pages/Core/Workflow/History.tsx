import Pagination from '@/Components/Qhsse/Pagination';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Paginated } from '@/types/core';
import { Head, router } from '@inertiajs/react';
import { FormEvent, useState } from 'react';
interface History { id: number; module_name: string; reference_id: number; from_status?: string | null; to_status: string; action_key: string; action_label: string; reason?: string | null; actor?: { id: number; name: string; email: string } | null; created_at: string; }
export default function History({ histories, filters }: { histories: Paginated<History>; filters: { module_name?: string; reference_id?: string } }) { const [moduleName,setModuleName]=useState(filters.module_name ?? ''); const [referenceId,setReferenceId]=useState(filters.reference_id ?? ''); function submit(e: FormEvent){e.preventDefault(); router.get(route('core.workflow.history'),{module_name:moduleName,reference_id:referenceId},{preserveState:true});} return <AuthenticatedLayout header={<h2 className="text-xl font-semibold text-gray-800 dark:text-gray-200">Workflow History</h2>}><Head title="Workflow History"/><div className="py-12"><div className="mx-auto max-w-7xl space-y-6 sm:px-6 lg:px-8"><form onSubmit={submit} className="flex gap-2"><input className="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-100" value={moduleName} onChange={(e)=>setModuleName(e.target.value)} placeholder="module_name"/><input className="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-100" value={referenceId} onChange={(e)=>setReferenceId(e.target.value)} placeholder="reference_id"/><button className="rounded-md bg-gray-900 px-4 py-2 text-white dark:bg-gray-100 dark:text-gray-900">Search</button></form><div className="overflow-hidden bg-white shadow-sm sm:rounded-lg dark:bg-gray-800"><table className="min-w-full divide-y divide-gray-200 dark:divide-gray-700"><thead className="bg-gray-50 dark:bg-gray-900"><tr><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">Reference</th><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">Transition</th><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">Actor</th><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">Reason</th></tr></thead><tbody className="divide-y divide-gray-200 dark:divide-gray-700">{histories.data.map((history)=><tr key={history.id}><td className="px-4 py-3 text-sm text-gray-900 dark:text-gray-100">{history.module_name} #{history.reference_id}</td><td className="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{history.from_status ?? '-'} → {history.to_status}<div className="text-xs text-gray-500">{history.action_label}</div></td><td className="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{history.actor?.name ?? '-'}</td><td className="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{history.reason ?? '-'}</td></tr>)}</tbody></table></div><Pagination links={histories.links}/></div></div></AuthenticatedLayout> }
