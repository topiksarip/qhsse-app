import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import { Head, Link } from '@inertiajs/react';
interface Transition { id: number; from_status: string; to_status: string; action_key: string; action_label: string; requires_reason: boolean; required_permission?: string | null; is_active: boolean; }
interface Definition { id: number; module_name: string; code: string; name: string; initial_status: string; transitions: Transition[]; }
export default function Show({ definition }: { definition: Definition }) { return <AuthenticatedLayout header={<h2 className="text-xl font-semibold text-gray-800 dark:text-gray-200">{definition.name}</h2>}><Head title={definition.name}/><div className="py-12"><div className="mx-auto max-w-5xl space-y-6 sm:px-6 lg:px-8"><Link href={route('core.workflow.index')} className="text-sm text-indigo-600 dark:text-indigo-400">Back to workflow</Link><div className="bg-white p-6 shadow-sm sm:rounded-lg dark:bg-gray-800"><div className="mb-4 text-sm text-gray-600 dark:text-gray-300">Module: {definition.module_name} | Initial: {definition.initial_status}</div><table className="min-w-full divide-y divide-gray-200 dark:divide-gray-700"><thead className="bg-gray-50 dark:bg-gray-900"><tr><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">From</th><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">Action</th><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">To</th><th className="px-4 py-3 text-left text-xs font-medium uppercase text-gray-500">Rules</th></tr></thead><tbody className="divide-y divide-gray-200 dark:divide-gray-700">{definition.transitions.map((transition)=><tr key={transition.id}><td className="px-4 py-3 text-sm text-gray-900 dark:text-gray-100">{transition.from_status}</td><td className="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{transition.action_label}<div className="text-xs text-gray-500">{transition.action_key}</div></td><td className="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{transition.to_status}</td><td className="px-4 py-3 text-sm text-gray-700 dark:text-gray-300">{transition.requires_reason ? 'Reason required' : 'No reason required'}{transition.required_permission && <div className="text-xs text-gray-500">{transition.required_permission}</div>}</td></tr>)}</tbody></table></div></div></div></AuthenticatedLayout> }
