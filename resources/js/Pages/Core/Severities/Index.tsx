import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';
import Pagination from '@/Components/Qhsse/Pagination'; import StatusBadge from '@/Components/Qhsse/StatusBadge';
import { Severity, Paginated } from '@/types/core'; import { Head, Link, router } from '@inertiajs/react'; import { FormEvent, useState } from 'react'; import DeleteWithConfirm from '@/Components/UI/DeleteWithConfirm';
import FilterPanel from '@/Components/UI/FilterPanel';
export default function Index({ items, filters }: { items: Paginated<Severity>; filters: { search?: string } }) { const [search,setSearch]=useState(filters.search ?? ''); function submit(e: FormEvent){e.preventDefault(); router.get(route('core.severities.index'),{search},{preserveState:true});} return <AuthenticatedLayout header={<h2 className="text-xl font-semibold text-gray-800 dark:text-gray-200">Severities</h2>}><Head title="Severities"/><div className="py-12"><div className="mx-auto max-w-7xl space-y-6 sm:px-6 lg:px-8"><FilterPanel activeCount={search ? 1 : 0}><form onSubmit={submit} className="flex gap-2"><input className="rounded-md border-gray-300 dark:border-gray-700 dark:bg-gray-900 dark:text-gray-100" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search severities"/><button className="rounded-md bg-gray-900 px-4 py-2 text-white dark:bg-gray-100 dark:text-gray-900">Search</button></form></FilterPanel><div className="flex justify-end"><Link href={route('core.severities.create')} className="rounded-md bg-indigo-600 px-4 py-2 text-white">New Severity</Link></div><div className="overflow-hidden bg-white shadow-sm sm:rounded-lg dark:bg-gray-800"><table className="min-w-full divide-y divide-gray-200 dark:divide-gray-700"><thead className="bg-gray-50 dark:bg-gray-900"><tr><th className="px-6 py-3 text-left text-xs font-medium uppercase text-gray-500">Code</th><th className="px-6 py-3 text-left text-xs font-medium uppercase text-gray-500">Name/Level</th><th className="px-6 py-3 text-left text-xs font-medium uppercase text-gray-500">Status</th><th className="px-6 py-3"/></tr></thead><tbody className="divide-y divide-gray-200 dark:divide-gray-700">{items.data.map((item:any)=><tr key={item.id}><td className="px-6 py-4 text-sm text-gray-900 dark:text-gray-100">{item.code ?? `${item.likelihood}x${item.consequence}`}</td><td className="px-6 py-4 text-sm text-gray-900 dark:text-gray-100">{item.name ?? item.level}</td><td className="px-6 py-4"><StatusBadge active={item.is_active}/></td><td className="px-6 py-4 text-right text-sm"><Link href={route('core.severities.edit', item.id)} className="text-indigo-600 dark:text-indigo-400">Edit</Link><DeleteWithConfirm routeName="core.severities.destroy" id={item.id} permission="core.severities.delete" itemLabel={item.name ?? item.level} redirectTo="core.severities.index" asLink>Delete</DeleteWithConfirm></td></tr>)}</tbody></table></div><Pagination links={items.links}/></div></div></AuthenticatedLayout>; }
